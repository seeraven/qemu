# VSCode Devcontainer

The directory `.devcontainer` contains the setup of a devcontainer for
VSCode. To open the dev-container, open the _Command Palette_ (Ctrl-Shift-P)
and select the command _Dev Containers: Reopen in Container_.
